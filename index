<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Gestão de Retângulos</title>
  <style>
    body {
      font-family: sans-serif;
      padding: 20px;
    }
    button {
      margin: 5px;
      padding: 8px 12px;
    }
    .container {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 20px;
    }
    .box {
      width: 150px;
      height: 100px;
      border: 2px solid #007bff;
      border-radius: 8px;
      padding: 5px;
      background: #e6f0ff;
      cursor: pointer;
      position: relative;
    }
    .box-title {
      font-weight: bold;
      margin-bottom: 5px;
    }
    .materials {
      font-size: 0.9em;
    }
    .modal {
      position: fixed;
      top: 20%;
      left: 50%;
      transform: translateX(-50%);
      background: white;
      border: 1px solid #ccc;
      padding: 20px;
      z-index: 10;
      box-shadow: 0 0 10px rgba(0,0,0,0.3);
    }
    .overlay {
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background: rgba(0,0,0,0.5);
      z-index: 5;
    }
  </style>
</head>
<body>
  <h1>Gerenciador de Retângulos</h1>
  <button onclick="addBox()">Adicionar Retângulo</button>
  <button onclick="renameBox()">Renomear Retângulo</button>
  <button onclick="removeBox()">Excluir Retângulo</button>

  <div class="container" id="boxContainer"></div>

  <!-- Modal para materiais -->
  <div id="modal" class="modal" style="display:none;">
    <h3 id="modalTitle">Materiais de:</h3>
    <ul id="materialList"></ul>
    <input type="text" id="materialInput" placeholder="Material">
    <input type="number" id="quantityInput" placeholder="Qtd" min="1">
    <button onclick="addMaterial()">Adicionar</button>
    <button onclick="closeModal()">Fechar</button>
  </div>
  <div id="overlay" class="overl
